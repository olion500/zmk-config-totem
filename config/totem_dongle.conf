# APDS9960 ambient light sensor not responding on this dongle's board variant —
# disable to stop 100ms i2c sample failures that flood the log/work queue
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=70
